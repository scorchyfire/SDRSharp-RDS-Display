name: Build and Release (Latest Version)

on:
  push:
    branches: ["latest-version"]
  pull_request:
    branches: ["latest-version"]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "9.0.x"

      - name: Build (Release)
        run: dotnet build sdrplugins/sdrplugins.sln --configuration Release

      - name: Read version
        id: version
        shell: pwsh
        run: |
          $version = (Get-Content "version.txt" -Raw).Trim()
          if ([string]::IsNullOrWhiteSpace($version)) {
            throw "version.txt is empty"
          }
          "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Create release zip
        shell: pwsh
        run: |
          $dllPath = Get-ChildItem -Path "sdrplugins/Release" -Recurse -File -Filter "SDRSharp.RdsDisplay.dll" | Select-Object -First 1 -ExpandProperty FullName
          $jsonPath = Get-ChildItem -Path "sdrplugins/Release" -Recurse -File -Filter "pi_codes.json" | Select-Object -First 1 -ExpandProperty FullName
          if (-not $dllPath -or -not $jsonPath) {
            throw "Could not find build outputs under sdrplugins/Release."
          }
          $zipName = "SDRSharp_v1912andnewer-RDS-Display-${{ steps.version.outputs.version }}.zip"
          Compress-Archive -Path $dllPath, $jsonPath -DestinationPath $zipName

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: SDRSharp_v1912andnewer-RDS-Display-${{ steps.version.outputs.version }}
          path: SDRSharp_v1912andnewer-RDS-Display-${{ steps.version.outputs.version }}.zip

      - name: Check release eligibility
        if: github.event_name == 'push'
        id: release_gate
        shell: pwsh
        run: |
          $currentVersion = "${{ steps.version.outputs.version }}"
          $before = "${{ github.event.before }}"
          $shouldRelease = $false

          if ($before -and $before -ne "0000000000000000000000000000000000000000") {
            $changed = git diff --name-only $before "${{ github.sha }}" -- version.txt
            if ($changed) {
              $versionExistsInBefore = git ls-tree --name-only $before -- version.txt
              $previousVersion = if ($versionExistsInBefore) { (git show "${before}:version.txt").Trim() } else { "" }
              if ($previousVersion -ne $currentVersion) {
                $shouldRelease = $true
              }
            }
          }

          if (-not $shouldRelease) {
            Write-Host "Skipping release: version.txt not changed to a different version."
          }

          "should_release=$($shouldRelease.ToString().ToLowerInvariant())" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Create GitHub Release
        if: github.event_name == 'push' && steps.release_gate.outputs.should_release == 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: ${{ steps.version.outputs.version }} - SDRSharp >=v1912
          target_commitish: ${{ github.sha }}
          files: SDRSharp_v1912andnewer-RDS-Display-${{ steps.version.outputs.version }}.zip
